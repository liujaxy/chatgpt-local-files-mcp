# 隐私与访问边界

工具读取的正文和图片会通过隧道传给获准的远端客户端。请只选择适合该远端服务处理的资料目录。

- 一次只开放一个用户选定的项目，拒绝越界、盘符路径、NTFS 备用数据流、符号链接和目录联接，并验证已打开的 Windows 文件句柄。
- 每次启动创建访问标识，停止时撤销并结束子进程。不提供写文件或任意命令执行工具。
- state/ 含加密凭据、偏好、会话及日志。加密文件也不能公开；同一 Windows 用户身份下的程序可能解密 DPAPI 凭据。
- 搜索跳过目录不等于保密策略。选定根目录内的受支持文件可能被直接读取；不要放入含秘密的 Markdown 或截图。
- project_info 会读取根目录 AGENTS.md、PROJECT_STATUS.md 开头，请先检查其中是否含个人信息。
- 停止不能收回已进入远端聊天的内容，切换项目请新建聊天。

## 仓库和问题反馈

不提交运行密钥、真实隧道 ID、state、日志、个人路径或私人研究资料。反馈使用最小化的脱敏示例。若仓库启用 GitHub 私密漏洞报告，可使用该入口；勿在公开 Issue 中提交秘密或可直接利用的漏洞细节。

公开前检查 Git 暂存内容与历史，不只依赖忽略规则。密钥泄露后应先向服务提供方撤销并更换；删除当前文件不能消除已公开的历史。
