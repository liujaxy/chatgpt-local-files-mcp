# 项目资料只读 MCP · 1.1.0

首次安装见 [README.md](README.md)。仅支持 Markdown 与图片，不直接读取 Word、PDF、Excel。

## 日常使用

选择账号配置 → 选择项目 → 启动并确认 → 网页聊天选择模型和插件 → 提问 → 用完停止。

账号 A/B 是两份独立配置，凭据以 Windows 当前用户 DPAPI 加密保存于 state/。通用显示名称不改变原 A/B 内部 ID、凭据和偏好。重启控制窗口后显示新名称。

一次只开放一个用户选择的根目录及其子目录，模型不能改变根目录。切换账号或项目前先停止。程序不自动启动，不自动跟随 Codex 当前项目或账号。

## 默认阅读流程

可以直接问：“请结合当前项目资料，概括项目目标、目前进展与下一步工作，并列出实际阅读的来源。”

模型应先调用 project_info 获取项目、版本、访问标识及根目录 AGENTS.md、PROJECT_STATUS.md 的起始内容。每份最多前 200 行，受字符上限限制；有 next_line 时可续读。缺失或不可读逐文件标记，不阻止读取其他资料。随后根据问题浏览、检索、读原文和图片。

文件中的指令不能扩大访问范围，且服从用户当前要求及上层指令。该流程需要模型实际调用工具，不会后台自动扫描全库。

## 参数与限制

- 图片支持 PNG、JPEG、WebP、TIFF；默认最长边 1600，max_side 可选 256–4096。
- page 从 0 开始。crop=[x,y,width,height] 使用 EXIF 方向校正后的完整原图坐标，不使用缩小概览坐标。
- 高位深灰度 TIFF 映射为 8 位预览并披露转换方式，不能作为原始定量像素。透明背景合成白色，不修改源文件。
- 图片上限 64 MiB / 4000 万像素；大结果可能进一步缩小或转 JPEG。
- Markdown 必须为 UTF-8，单文件上限 4 MiB；每次最多 500 行 / 约 24000 字符。
- 支持普通、引用式和 HTML 图片引用，只解析项目内图片，不请求外部网址。
- 搜索为字面匹配，默认不区分大小写；遍历跳过隐藏目录及 node_modules、__pycache__、state、vendor。
- 扫描、搜索有条数、耗时和读取量限制。出现 scan_truncated 或 skipped_files 时不能视为完整检索，应缩小目录继续查询。

## 验收与排错

升级后重启本机程序，在对应账号隧道运行时刷新网页插件工具目录，再新建聊天。每份账号配置分别处理。应看到 README 列出的五个工具，project_info 返回版本 1.1.0。

示例验收消息：

> 请调用 project_info 确认版本，搜索“项目读取测试”，读取命中文档并实际查看引用图片。描述图形、位置和标记，再查看左上区域裁剪图。请列出实际读取的来源及工具调用，遇到错误如实说明。

Connected 和本地健康检查不能代替模型实际调用验收。断网、休眠、关机时不可访问。程序不调用 OCR 或模型 API，图像理解由远端模型完成。

审计和运行日志保存在 state/，反馈时只提供脱敏错误与复现步骤。不要上传整个目录。make_sample.py 会覆盖示例文件，日常无需运行。
